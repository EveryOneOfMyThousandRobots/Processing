ArrayList<PVector> circle = new ArrayList<PVector>();
ArrayList<PVector> square = new ArrayList<PVector>();
float radius, cx, cy;
void setup() {
  size(500,500);
  
  radius = width / 3;
  cx = width / 2;
  cy = height / 2;
  
}

void draw(){
}