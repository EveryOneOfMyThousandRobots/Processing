class HC {
  PVector pos, dims;
  final int order;
  ArrayList<PGraphics> tiles;
  ArrayList<HC> children;
  
  HC(float x, float y, float w, float h, int order) {
    this.order = order;
    this.pos = new PVector(x,y);
    this.dims = new PVector(w,h);
  }
  
  

}

void setup() {
}

void draw() {
  
}
